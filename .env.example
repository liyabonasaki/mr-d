# Copy this file to .env and fill in your values
DATABASE_URL=postgresql://postgres:password@localhost:5432/mrd_orders
WORKER_POLL_INTERVAL=2        # seconds between outbox poll cycles
WORKER_BATCH_SIZE=50          # max outbox events processed per cycle
